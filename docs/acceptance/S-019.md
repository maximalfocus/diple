# S-019 — Every verified host drives itself

The acceptance cases for issue #54. R-014 claims the four hosts something can
type into from outside, and holds Ghostty, iTerm2 and Terminal.app: still
passed through, still replayed, claimed by no list and demanded by no gate,
until S-020 verifies them by hand.

| Case | Level | Exercise | Passes when |
|---|---|---|---|
| T-01 | unit | Evidence covering the four claimed hosts and no held host, then the same with one claimed host missing | The gate passes on the first and names the missing claimed host on the second; no failure ever names a held host |
| T-02 | unit | A committed-capture set complete, then missing a held host's mode | Tier 3 passes on the first and names the held host on the second: a held host keeps replaying |
| T-03 | unit | The clipboard ladder in Terminal.app | It is still excluded from OSC 52: passing a host through is not claiming it |
| H-01 | host, driven | Tiers 4 and 5 for WezTerm, kitty, `tmux` and `herdr` on Linux amd64 and macOS arm64 | Every one passes in both drawing modes with nobody at either keyboard and the macOS screen locked |
